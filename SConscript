Import('environment')

environment['PY_PACKAGES'] += []
environment['CONFIG_FILES'] += []
environment['DATA_FILES'] += \
    [Dir('character_scripts'), Dir('dialogue'), Dir('fonts'), Dir('gui'),
     Dir('maps'), Dir('music'), Dir('objects'), Dir('quests')]
environment['EXECUTABLES'] += []

